<!DOCTYPE html>

<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmar Pago - Webpay Plus</title>
    <link href="/css/styles.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/tailwind.css">
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full bg-gray-800 rounded-lg shadow-lg p-6">
        <h1 class="text-2xl font-bold mb-4 text-center">Confirmar Pago</h1>
        <p class="mb-2"><span class="font-semibold">Orden de Compra:</span> <%= buyOrder %></p>
        <p class="mb-2"><span class="font-semibold">ID de Sesión:</span> <%= sessionId %></p>
        <p class="mb-4"><span class="font-semibold">Monto Total:</span> $<%= amount %></p>

        <!-- Botón para confirmar el pago -->
        <form action="/webpay_plus/create" method="POST" class="text-center">
            <button type="submit" class="px-6 py-2 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded-md w-full mt-4">
                Pagar con Webpay
            </button>
        </form>
    </div>
</body>
</html>